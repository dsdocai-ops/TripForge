# Copy this file to .env for local development.
# .env is gitignored — never commit real secrets.

# Routes Claude calls through /api/claude (Vercel serverless function).
# Set ANTHROPIC_API_KEY (no VITE_ prefix) in Vercel dashboard for production.
VITE_PROXY_URL=/api/claude
ANTHROPIC_API_KEY=sk-ant-...
