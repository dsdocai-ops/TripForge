import React from 'react'
import ReactDOM from 'react-dom/client'
import TripForge from './TripForge'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TripForge />
  </React.StrictMode>
)
